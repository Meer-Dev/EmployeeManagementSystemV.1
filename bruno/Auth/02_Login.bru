meta {
  name: 02_Login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/auth/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "admin@example.com",
    "password": "Admin123!"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
