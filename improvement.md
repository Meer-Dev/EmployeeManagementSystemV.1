super admin cant be inactive
super admin can make other admin inactive / active
inactive users/admins cant login they should get notification that they are no longer admin
idompotentkey/corelationid/refreshtoken/RateLimiting all should be working/
different dashboards boards for admins and users
offset and jump to page in front end if user can customize pagesize 20, 50, 100 per page options in dropdown, user can jump to pagenumber but if page number is greater than 100 then pagenumber 100 should open and after that cursor/keyset button load more should arrive on page 100 and infinite scroll happen.
employees can be search by their first name and last name/department/email there is no dynamic search option so only one could be enter by both admins/users
employees can be made active/inactive by only admins not by users
